# Monoprix Ticket Downloader

Downloads Monoprix receipt PDFs from:

https://client.monoprix.fr/monoprix-shopping/tickets

The script connects to a normal Windows Chrome instance using Chrome DevTools Protocol.

## Install

```powershell
python -m pip install --user -r requirements.txt